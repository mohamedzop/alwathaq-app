# الوثاق للصرافة — حزمة النشر العامة (GitHub Pages)

هذه الحزمة هي **الموقع العام فقط** — مخصصة للجمهور، **بدون أي تسجيل دخول**.
كل ملفات الموقع موجودة في جذر هذه الحزمة (index.html + style.css + assets/ ...).

## خطوات النشر (GitHub Pages)

1. افتح مستودعك على GitHub (الذي يحوي حالياً صفحة الدخول الخاطئة) — مثلاً `mohamedzop/alwathaq`.
2. احذف الملفات القديمة من المستودع: `index.html` و `app.js` و `style.css` وغيرها.
3. ارفع **كل محتويات هذه المجلد** (الملفات والمجلدات داخله) إلى **جذر المستودع** مباشرة — لا تضعها في مجلد فرعي.
4. Settings ← Pages ← Source: **Deploy from a branch** ← الفرع `main` ← المجلد `/ (root)` ← Save.
5. انتظر 1-2 دقيقة، ثم افتح `https://<اسمك>.github.io/alwathaq/` — سترى الموقع العام مباشرة.

## ملاحظات مهمة

- **لا يوجد تسجيل دخول في هذه النسخة** — رابط «دخول الموظفين» لا يظهر على GitHub Pages أصلاً (يظهر فقط عندما يكون الخادم شغّالاً).
- الأسعار تظهر من جدول الشركة عبر JSONP، وعند تعذره تظهر القيم الاحتياطية في `rates.js`.
- **بوابة الموظفين (التي تطلب اسم المستخدم وكلمة المرور) لا يمكن أن تعمل على GitHub Pages** لأنه موقع ثابت بلا خادم وقاعدة بيانات. بوابة الموظفين تعمل فقط على استضافة تشغّل Node.js (مثل Render أو Railway) — استخدم الحزمة الكاملة `alwathaq-site-full.zip` مع ملف `render.yaml` واتبع `DEPLOY.md` بداخلها.

## للتشغيل المحلي (تجربة سريعة)

```bash
node server.js
```

ثم افتح http://localhost:8123 — هذا الخادم يعرض الموقع العام على `/` وبوابة الموظفين على `/portal` (admin/admin123).