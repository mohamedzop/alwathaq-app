# 🗄️ قاعدة بيانات الوثاق للصرافة — التصميم

قاعدة البيانات مبنية على **SQLite** داخل التطبيق نفسه (`data/alwathaq.db`) — تنشأ تلقائياً عند أول تشغيل، بلا أي إعداد أو سيرفر خارجي. هذا التصميم يطابق الخطة المتفق عليها:

> جدول العملاء · جدول المعاملات (نوع، مبلغ، تاريخ، الموظف المسؤول) · جدول المستخدمين (موظف/مدير) بصلاحيات مختلفة.

## الجداول

### 1) `users` — المستخدمون (الموظفون والمديرون)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INTEGER PK | رقم تلقائي |
| username | TEXT UNIQUE | اسم الدخول |
| full_name | TEXT | الاسم الكامل |
| password_hash | TEXT | كلمة مرور مشفّرة (scrypt + ملح) — لا تُخزَّن نصاً أبداً |
| role | TEXT | `manager` (مدير) أو `employee` (موظف) |
| active | INTEGER | مفعّل (1) / معطّل (0) |
| created_at | TEXT | تاريخ الإنشاء |

### 2) `clients` — العملاء
| الحقل | النوع | الوصف |
|---|---|---|
| id | INTEGER PK | رقم تلقائي |
| full_name | TEXT | الاسم الكامل |
| phone | TEXT | رقم الهاتف |
| id_number | TEXT | رقم الهوية / الجواز |
| client_type | TEXT | `individual` (فرد) أو `company` (شركة/مؤسسة) |
| notes | TEXT | ملاحظات (عميل دائم، تعامل بالجملة…) |
| created_at | TEXT | تاريخ الإضافة |

### 3) `transactions` — المعاملات (سجل العمليات اليومية)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INTEGER PK | رقم تلقائي |
| tx_type | TEXT | نوع العملية: `buy` شراء · `sell` بيع · `local_transfer` حوالة داخلية · `intl_transfer` حوالة دولية · `voda_cash` فودافون كاش |
| currency | TEXT | العملة (USD, EUR, GBP, TRY, AED, EGP, TND, LYD, VODA) |
| amount | REAL | المبلغ بالعملة |
| rate | REAL | سعر الصرف (1 للحوالات/الشحن بالدينار) |
| lyd_value | REAL | القيمة بالدينار = المبلغ × السعر |
| client_id | INTEGER FK→clients | العميل المسجّل (اختياري) |
| client_name | TEXT | اسم العميل (منسوخ للعابرين/غير المسجلين) |
| client_phone | TEXT | هاتفه |
| user_id | INTEGER FK→users | **الموظف المسؤول** (يُسجَّل تلقائياً) |
| note | TEXT | ملاحظة (فئة الورق، وجهة الحوالة…) |
| created_at | TEXT | التاريخ والوقت تلقائياً |

### 4) `cash_days` — إقفال اليوم / جرد النقد
| الحقل | النوع | الوصف |
|---|---|---|
| id | INTEGER PK | رقم تلقائي |
| day | TEXT UNIQUE | اليوم `YYYY-MM-DD` |
| opening_lyd | REAL | النقد المتوفر صباحاً بالدينار |
| counted_lyd | REAL | النقد المعدود عند الإقفال (فارغ = لم يُقفل بعد) |
| closed_at | TEXT | وقت الإقفال |
| notes | TEXT | ملاحظات |
| updated_at | TEXT | آخر تعديل |

### 5) `cash_adjustments` — تعديلات نقدية (إيداع / سحب)
| الحقل | النوع | الوصف |
|---|---|---|
| id | INTEGER PK | رقم تلقائي |
| day | TEXT FK→cash_days | اليوم |
| adj_type | TEXT | `in` إيداع (زيادة الصندوق) · `out` سحب (نقص الصندوق) |
| amount | REAL | المبلغ |
| note | TEXT | البيان |
| created_at | TEXT | الوقت |

### 6) `rates` — أسعار الصرف (مصدر الأسعار الأساسي)
| الحقل | النوع | الوصف |
|---|---|---|
| key | TEXT PK | مفتاح السعر: `USD_cash` · `USD_bank` · `USD_card` · `EUR_cash` · `EUR_bank` · `EUR_card` · `VODAFONE_cash` · `VODAFONE_bank` … |
| retail | REAL | سعر القطاعي (كم دينار ليبي مقابل وحدة العملة) |
| wholesale | REAL | سعر الجملة |
| updated_by | INTEGER FK→users | الموظف الذي عدّل السعر آخر مرة |
| updated_at | TEXT | وقت آخر تعديل |

### 7) `meta` — بيانات وصفية
| الحقل | النوع | الوصف |
|---|---|---|
| k | TEXT PK | المفتاح (مثال: `rates_imported_at` — آخر استيراد من جدول الشركة) |
| v | TEXT | القيمة |

## إقفال اليوم — منطق المطابقة
المتوقع (نقد الدينار آخر اليوم) يُحسب تلقائياً:

```
المتوقع = رصيد البداية + أثر المعاملات + الإيداعات − السحوبات
الفرق   = النقد المعدود − المتوقع
```
الأثر النقدي لكل نوع عملية على صندوق الدينار:
- `buy` شراء عملة أجنبية → دينار **يخرج** (− قيمة الدينار)
- `sell` بيع عملة أجنبية → دينار **يدخل** (+ قيمة الدينار)
- `local_transfer` / `intl_transfer` / `voda_cash` → دينار **يدخل** (+ قيمة الدينار)

## الصلاحيات (كما في الخطة)
- **موظف** `employee` → يسلّج الدخول، يضيف معاملات وعملاء، يبحث في سجل العميل، **ويعدّل أسعار الصرف** من صفحة «الأسعار». **لا يرى** التقارير الإجمالية ولا صفحة المستخدمين.
- **مدير** `manager` → كل ما سبق + التقارير اليومية/الأسبوعية + إدارة المستخدمين (إنشاء، صلاحيات، تفعيل/تعطيل، تغيير كلمات المرور) + **استيراد الأسعار من جدول الشركة** (يحل محل أي تعديل يدوي).
- الحماية على الخادم نفسه: أي طلب لواجهة المدير من حساب موظف يُرفض بـ `403` حتى لو عُدّل الكود في المتصفح.

## أهم نقاط الواجهات (API)
| المسار | الصلاحية | الوظيفة |
|---|---|---|
| `POST /api/login` · `POST /api/logout` · `GET /api/me` | عام | تسجيل الدخول (جلسة آمنة HttpOnly) |
| `GET /api/summary` | موظف+مدير | ملخص (عدد، إجمالي الدينار) لأي فترة |
| `GET/POST /api/transactions` | موظف+مدير | عرض / تسجيل معاملة |
| `GET/POST /api/clients` · `GET /api/clients/:id` | موظف+مدير | بحث/إضافة عميل + سجل معاملاته |
| `GET /api/reports/summary` · `/daily` | **مدير فقط** | ملخص حسب النوع والعملة، وتوزيع يومي |
| `GET /api/rates` | عام | أسعار اليوم (يقرؤها الموقع العام والحاسبة) |
| `PUT /api/rates` | موظف+مدير | حفظ تعديل الأسعار مباشرة (بدون Google Sheets) |
| `POST /api/rates/import` | **مدير فقط** | استيراد لمرة واحدة من جدول الشركة (يستبدل الأسعار الحالية) |
| `GET/POST /api/settlement` · `POST/DELETE /api/settlement/adjustments` | **مدير فقط** | إقفال اليوم / جرد النقد ومطابقته |
| `GET/POST /api/users` · `PATCH /api/users/:id` | **مدير فقط** | إدارة المستخدمين |

## ملاحظات للنشر
- الملف `data/alwathaq.db` يُنشأ عند أول تشغيل؛ على منصات مثل **Render/Railway** اربط **قرصاً دائماً (Persistent Disk)** بالمجلد `data/` حتى لا تُمسح البيانات عند كل إعادة نشر.
- لحذف كل المعاملات والعملاء وإبقاء المستخدمين: أوقف الخادم ثم شغّل `node seed-demo.mjs` (يعيد زرع بيانات تجريبية)، أو احذف ملف القاعدة ليُعاد إنشاؤها نظيفة.
